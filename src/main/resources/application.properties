spring.application.name=transpo 

spring.datasource.url=jdbc:postgresql://db.rrbasuzjumbqcsrhdytw.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=\#Ravindu1102

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway migrations (disabled due to PostgreSQL 18 compatibility)
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration

spring.thymeleaf.cache=false
server.port=8080

# Add these lines
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.session=DEBUG
logging.level.org.springframework.web=DEBUG

# Disable thymeleaf (if not using it)
spring.thymeleaf.enabled=false

# Force session creation
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false